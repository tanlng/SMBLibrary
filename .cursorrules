# SMBLibrary 项目规则

## 项目概述
这是 SMBLibrary，一个开源的 C# SMB 1.0/CIFS、SMB 2.0、SMB 2.1 和 SMB 3.0 服务器和客户端协议实现。该项目支持 NetBIOS over TCP（端口 139）和直接 TCP 托管（端口 445）。

## 技术栈
- **编程语言**: C# (.NET 8.0)
- **协议**: SMB 1.0/CIFS, SMB 2.0, SMB 2.1, SMB 3.0
- **身份验证**: NTLM, GSSAPI, 集成 Windows 身份验证
- **传输**: NetBIOS over TCP, 直接 TCP
- **依赖项**: Newtonsoft.Json, 自定义工具库

## 项目结构
- `SMBLibrary/` - 核心库实现
- `SMBLibrary.Win32/` - Windows 特定实现
- `SMBLibrary.Adapters/` - IFileSystem 到 INTFileStore 适配器
- `SMBLibrary.Tests/` - 单元测试和集成测试
- `SMBServer/` - 示例服务器应用程序
- `Utilities/` - 共享工具函数

## 代码风格和标准

### C# 编码约定
- 遵循 Microsoft C# 编码约定
- 公共成员、类、方法和属性使用 PascalCase
- 私有字段和局部变量使用 camelCase
- 使用有意义、描述性的变量和方法名称
- 在协议实现中优先使用显式类型而非 `var` 以提高清晰度

### SMB 协议特定指南
- **协议合规性**: 所有 SMB 协议实现必须严格遵循官方 SMB 规范
- **错误处理**: 对 SMB 协议错误使用适当的 NTStatus 代码
- **安全性**: 为 SMB 2.0/3.0 实现适当的身份验证和加密
- **兼容性**: 保持与旧版 SMB 版本的向后兼容性
- **性能**: 优化网络效率和最小内存分配

### 文件组织
- 在适当的命名空间中组织相关功能
- 将协议特定代码保存在专用文件夹中（SMB1/、SMB2/ 等）
- 清楚分离客户端和服务器实现
- 使用辅助类处理常见操作

### 测试要求
- 为所有协议解析和序列化编写单元测试
- 包含身份验证流程的集成测试
- 测试客户端和服务器场景
- 验证与 Windows SMB 实现的兼容性

### 文档标准
- 使用 XML 注释记录所有公共 API
- 在适用时包含协议规范引用
- 为复杂操作提供使用示例
- 记录安全考虑和身份验证流程

### 版权声明规则
- **新创建的文件**: 不添加任何版权声明，直接开始代码
- **修改现有文件**: 保留原有版权声明，不添加新的版权信息
- **遵循项目惯例**: 保持与现有代码风格一致
- **许可证**: 所有代码遵循项目的开源许可证（LGPL）

### 语言使用规则
- **代码注释**: 使用英文编写所有代码注释（XML注释、行内注释、TODO等）
- **文档文件**: 使用中文编写所有文档（.md文件、README等）
- **变量和方法名**: 使用英文命名
- **错误消息**: 使用英文编写异常和错误消息
- **日志消息**: 使用英文编写日志记录消息

### 日志记录规则
- **日志级别**: 使用 `Utilities.Severity` 枚举（Critical, Error, Warning, Information, Verbose, Debug, Trace）
- **日志方法**: 使用 `LogToServer(Severity severity, string message)` 或 `LogToServer(Severity severity, string message, params object[] args)`
- **日志格式**: 使用英文消息，包含必要的上下文信息
- **性能考虑**: 避免在热路径中使用 Verbose 或更高级别的日志
- **错误处理**: 所有异常和错误情况必须记录日志
- **调试信息**: 使用 Debug 或 Trace 级别记录详细的调试信息

### 文档目录规范
- **技术文档**: 使用 Markdown 格式，结构清晰，层次分明
- **API 文档**: 使用 XML 注释，包含参数说明、返回值、异常情况
- **协议文档**: 详细描述协议实现细节，包含数据包格式和状态转换
- **用户指南**: 提供步骤说明和示例，便于用户理解和使用
- **变更日志**: 记录版本更新内容，包含新增功能、修复问题和破坏性变更
- **安全文档**: 详细说明安全考虑、身份验证流程和最佳实践
- **性能文档**: 提供性能指标、优化建议和基准测试结果

### 文档目录结构
```
docs/
├── api/                    # API 文档
│   ├── client/            # 客户端 API
│   ├── server/            # 服务器 API
│   └── protocols/         # 协议 API
├── protocols/             # 协议规范文档
│   ├── smb1/              # SMB 1.0/CIFS 协议
│   ├── smb2/              # SMB 2.0/2.1 协议
│   └── smb3/              # SMB 3.0 协议
├── guides/                # 用户指南
│   ├── getting-started/   # 入门指南
│   ├── authentication/    # 身份验证指南
│   └── troubleshooting/   # 故障排除指南
├── security/              # 安全文档
├── performance/           # 性能文档
├── changelog/             # 变更日志
└── examples/              # 示例代码和配置
```

### 文档命名规范
- 使用小写字母和连字符分隔（kebab-case）
- 文件名应具有描述性，如 `smb2-authentication.md`
- 目录名使用复数形式，如 `guides/`、`examples/`
- 版本相关文档使用版本号，如 `v1.5.4-changelog.md`

## 开发指南

### 协议实现
- 在处理之前始终验证传入的 SMB 数据包
- 优雅地处理格式错误的数据包并提供适当的错误响应
- 实现适当的会话管理和连接状态跟踪
- 根据需要支持 Unicode 和 ASCII 字符串编码

### 安全考虑
- 永远不要记录敏感的身份验证数据
- 对所有用户提供的数据实现适当的输入验证
- 在加密操作中使用安全的随机数生成
- 遵循文件系统访问的最小权限原则

### 性能优化
- 在热路径中最小化内存分配
- 使用高效的数据结构进行协议解析
- 为网络 I/O 操作实现适当的缓冲
- 考虑对 I/O 绑定操作使用 async/await 模式

### 错误处理
- 对不同错误条件使用特定的异常类型
- 提供有意义的错误消息用于调试
- 在适当的级别记录错误（Debug、Info、Warning、Error）
- 优雅地处理网络断开连接

## 常见模式

### SMB 命令实现
```csharp
public class CommandName : SMB1Command
{
    public override void ReadCommandBytes(byte[] buffer, int offset)
    {
        // 解析命令特定字段
    }
    
    public override void WriteCommandBytes(byte[] buffer, int offset)
    {
        // 序列化命令特定字段
    }
}
```

### 身份验证流程
- 实现适当的挑战-响应机制
- 支持 NTLM 和 Kerberos 身份验证
- 处理会话密钥生成和管理
- 根据适当的存储验证用户凭据

### 文件系统集成
- 为自定义文件系统实现 IFileSystem 接口
- 支持同步和异步操作
- 正确处理文件锁定和共享模式
- 实现适当的目录枚举

## 调试和故障排除
- 使用网络数据包捕获工具进行协议调试
- 为身份验证流程启用详细日志记录
- 使用各种 Windows 版本和 SMB 客户端进行测试
- 验证与不同网络配置的兼容性

## 依赖项和外部库
- 最小化外部依赖项
- 使用维护良好、经过安全审计的库
- 记录任何第三方依赖项及其用途
- 保持依赖项更新以获得安全补丁

## 版本兼容性
- 保持与 Windows NT 4.0 及更高版本的兼容性
- 支持 32 位和 64 位架构
- 使用各种 .NET Framework 和 .NET Core 版本进行测试
- 在版本更新中记录任何破坏性更改
